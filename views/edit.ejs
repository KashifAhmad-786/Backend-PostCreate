<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="w-full flex justify-end p-4">
        <a class="bg-red-500 rounded px-3 py-2 text-sm mb-5" href="/logout">Logout</a>
    </div>
    <div class="max-w-2xl mx-auto p-4">
        <!-- Header Section -->
        <div class="mb-8">
            <p class="text-gray-400">Edit Your Post</p>
        </div>

        <!-- Create Post Section -->
        <div class="mb-8">
            <form class="space-y-4" action="/update/<%= post._id %>" method="post">
                <!-- Post Textarea -->
                <textarea 
                    class="w-full resize-none px-4 py-3 bg-zinc-800 text-white border border-zinc-700 rounded-lg focus:outline-none focus:border-blue-500 transition placeholder-gray-500"
                    placeholder="What's on your mind ?"
                    rows="4"
                    name="content"
                ><%= post.content %></textarea>

                <!-- Create New Post Button -->
                <button 
                    type="submit" 
                    class="px-6 py-2 bg-yellow-600 text-white font-semibold rounded-lg transition duration-200"
                >
                    Update Post
                </button>
            </form>
        </div>
    </div>
</body>
</html>